# Current Feature: shadcn/ui Preset + Component Library + Theme System

## Status
In Progress

## Goals
- Apply shadcn preset `bKcD70rQG` for consistent design tokens.
- Inject theme tokens (colors, radius, fonts) into `globals.css`.
- Install and configure `next-themes` for dark mode support.
- Implement `ThemeProvider`, `ThemeToggle`, and `Providers` wrapper.
- Install core shadcn/ui components.

## Notes
- Using shadcn CLI for preset application.
- Theme system includes both light and dark mode CSS variables.

## History
- 2026-05-13: Initialized feature branch `chore/project-scaffold-ui` and updated documentation.
